Git commands practice 
git config --global user.name ""
git config --global user.email ""
mhdir smapleRepo
cd sampleRepo
git init
touch add.cpp
git add add.cpp
git commit -m "adding add.cpp"
git log


________________Prominent yesterday's tut____________

git clone https://github.com/anahatdeshmukh/gitvcs.git
ls
cd gitvcs
ls
git status
git status
git status
git add .
git status
git commit -m "Add python file"
git log
git push

_____________________________________________


Here is the complete Git command flow (from identity → initialize repo → commit → push → pull).
Just copy-paste and replace your repo URL.

⸻

✅ 1. Set your identity (only once per device)

git config --global user.name "Your Name"
git config --global user.email "your-email@example.com"


⸻

✅ 2. Start a new project (inside your project folder)

git init


⸻

✅ 3. Check what changed

git status


⸻

✅ 4. Add all files to staging area

git add .


⸻

✅ 5. Commit your files

git commit -m "Initial commit"


⸻

✅ 6. Connect remote repo (GitHub)

Replace with your repo URL

git remote add origin https://github.com/username/repo-name.git


⸻

✅ 7. Push code to GitHub (first time)

git push -u origin main

If your default branch is master instead of main, then:

git push -u origin master


⸻

✅ 8. Daily workflow (after initial push)

Add → Commit → Push

git status
git add .
git commit -m "update"
git push


⸻

✅ 9. Pull latest changes from remote

git pull


⸻

OPTIONAL Branch Commands

git checkout -b new-branch      # create branch
git checkout main               # switch branch
git push -u origin new-branch   # push branch


⸻

OPTIONAL Identity Check

git config --global --list


⸻

If you want, I can also send:
	•	SSH key setup (no need to enter password every push),
	•	VS Code GUI shortcuts for Git.

Just say “ssh setup” or “show VS Code git workflow”.
